<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Pixora.Avalonia.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600"
             x:Class="Pixora.Avalonia.Views.RankingsView"
             x:DataType="vm:RankingsViewModel">

    <Design.DataContext>
        <vm:RankingsViewModel/>
    </Design.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FFF8F8F8" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                          Text="Rankings" 
                          FontWeight="SemiBold" 
                          FontSize="20"/>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                    <CalendarDatePicker SelectedDate="{Binding SelectedDate}"/>
                    <CheckBox Content="R-18" IsChecked="{Binding ShowR18}"/>
                    <Button Content="Load Rankings" Command="{Binding LoadRankingsCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Border Background="White" 
                    BorderBrush="#FFE0E0E0" 
                    BorderThickness="1" 
                    CornerRadius="8"
                    Margin="16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Padding="32">
                <StackPanel Spacing="16" HorizontalAlignment="Center">
                    <TextBlock Text="📊" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock Text="Rankings" 
                              FontWeight="SemiBold" 
                              FontSize="24" 
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="Daily, weekly, and monthly rankings" 
                              TextAlignment="Center" 
                              Opacity="0.7"/>
                    <Button Content="Load Rankings" 
                            Command="{Binding LoadRankingsCommand}"
                            HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" 
                Background="#FFF8F8F8" 
                Padding="16,8" 
                BorderBrush="#FFE0E0E0" 
                BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}"/>
                <ProgressBar Grid.Column="1" 
                             IsIndeterminate="{Binding IsLoading}" 
                             Width="100" 
                             IsVisible="{Binding IsLoading}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
